services:
  telegram-bot:
    build: .
    env_file:
      - .env
    volumes:
      - ./data/tokens:/app/data/tokens
    ports:
      - "127.0.0.1:8080:8080"
    restart: unless-stopped
    environment:
      - TZ=Asia/Seoul
